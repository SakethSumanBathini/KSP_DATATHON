# NOTE: Catalyst AppSail does NOT install these — they are bundled in ./vendor for deploy.
# These are for LOCAL development only.
flask>=3.0.0
networkx>=3.0
jellyfish>=1.0.0
faker>=40.0.0          # only needed to REGENERATE data (01_data_generator), not at runtime
# scikit-learn intentionally NOT used — replaced by 06_retrieval/pure_tfidf.py (290MB -> 0MB)
